This project runs with 3 containers:

- django server
- postgres
- redis

Whe trying to add image to posts it could be oserror,
so arialbd.ttf to be added to /usr/share/font/arialbd.ttf (on container)